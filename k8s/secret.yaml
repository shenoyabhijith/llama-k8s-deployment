# Secret Configuration
# This Secret contains sensitive configuration data for the LLAMA service.
# Note: Values should be base64 encoded in production.

apiVersion: v1
kind: Secret
metadata:
  name: llama-secrets
  namespace: llama-deployment
  labels:
    app: llama-server
    component: secrets
    version: "3.2"
type: Opaque
data:
  # API Authentication
  HUGGING_FACE_HUB_TOKEN: eW91cl9oZl90b2tlbg==  # Replace with your base64 encoded token
  
  # Security Configuration
  API_KEY: eW91cl9hcGlfa2V5  # API authentication key
  
  # SSL/TLS Certificates (if needed)
  TLS_CERT: ""  # Base64 encoded certificate
  TLS_KEY: ""   # Base64 encoded private key
  
  # Monitoring Integration
  PROMETHEUS_AUTH_TOKEN: ""  # Monitoring authentication
  
  # Cloud Provider Credentials (if needed)
  CLOUD_ACCESS_KEY: ""
  CLOUD_SECRET_KEY: ""